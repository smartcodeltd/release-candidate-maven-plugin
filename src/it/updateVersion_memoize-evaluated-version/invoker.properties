invoker.name         = Evaluates the {{ timestamp }} token only once, so that the parent and child pom versions match
invoker.description  = https://github.com/smartcodeltd/release-candidate-maven-plugin/issues/12

invoker.goals        = ${project.groupId}:${project.artifactId}:${project.version}:updateVersion
invoker.buildResult  = success
invoker.mavenOpts    =
invoker.debug        = false
invoker.nonRecursive = false
